# Local-development database credentials. Copy to `.env` (git-ignored).
# In AWS these are NOT used — the real values live in SSM Parameter Store and
# are generated by Terraform. This file only feeds the local docker-compose stack.
POSTGRES_USER=appuser
POSTGRES_PASSWORD=appsecret
POSTGRES_DB=appdb
