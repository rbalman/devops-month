# Lowest-precedence variables — sensible fallbacks the role ships with.
# The vars passed to the role in site.yml override these.
site_name: "Default Site"
worker_processes: 1
deploy_user: deploy
base_packages:
  - curl
