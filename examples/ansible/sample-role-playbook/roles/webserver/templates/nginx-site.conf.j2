# Managed by Ansible — rendered per host by the webserver role.
server {
    listen 80 default_server;
    server_name {{ inventory_hostname }};

    root {{ web_root }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
